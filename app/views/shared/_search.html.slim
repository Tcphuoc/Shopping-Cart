= form_with(url: "/search", class: "d-flex search-box") do |f|
  .input-group
    = f.text_field :search, class: "form-control", placeholder: "Search"
    = button_tag(type: "submit", class: "btn btn-light") do
      span
        i.fa.fa-search
